"""Local-first modules for the fake-news image verification project."""

# Keep package import lightweight. Import the required module explicitly, e.g.
# ``from news_guard.service import NewsVerificationService``.
